Looking at the verified update document against everything discussed, here's the precise delta — what's actually needed now versus what's already done.

---

## What's Confirmed Done (Don't Revisit)

The verified update confirms these are genuinely implemented, not just planned:

**Hibernation worker with Redis locking** — fully done, enterprise-grade. The race condition fix from the enterprise analysis is already in `hibernation_worker.py` with UUID-based locks.

**Hibernation savings calculation** — done. Uses real `cluster.monthly_cost`, strategy-specific efficiency multipliers. The $1,500 mock is gone.

**AtharvaAI circuit breaker** — done. ML degradation detection is in `pool_ranking_service.py:439-447`.

**Region-namespaced blacklist** — done. `risky_pools:{region}` is already implemented.

**Karpenter sync, termination monitor, auto-rebalancer** — all scheduled and running.

---

## What's Actually Still Needed

Cross-referencing the verified update's confirmed gaps against the 13 enterprise issues:

### Tier 1 — Will Break in Production Right Now

**Hibernation DB columns (30 minutes)**
This is the only thing that will cause an immediate runtime crash. The worker code references `cluster.is_hibernating` and `cluster.hibernation_state` but the columns don't exist. Everything else degrades gracefully; this fails hard.

```sql
-- 20260220_add_hibernation_columns.py
ALTER TABLE clusters ADD COLUMN is_hibernating BOOLEAN DEFAULT FALSE;
ALTER TABLE clusters ADD COLUMN hibernation_state JSONB DEFAULT NULL;
ALTER TABLE clusters ADD COLUMN hibernation_lock VARCHAR(255) DEFAULT NULL;
ALTER TABLE clusters ADD COLUMN hibernation_lock_acquired_at TIMESTAMP DEFAULT NULL;
```

Also update `backend/models/cluster.py` to add the SQLAlchemy column definitions, then `alembic upgrade head`. Nothing else works until this is done.

**Spot price Celery schedule pointing to mock (15 minutes)**
`backend/workers/app.py:88-91` calls `workers.atharvaai.collect_spot_prices` (mock). Change it to `backend.workers.tasks.pricing.fetch_aws_pricing`. This is literally a one-line change that unlocks real ML feature computation. The real scraper already exists and works — it's just not being called.

---

### Tier 2 — Financially Consequential (Wrong Numbers Shown to Users)

**Right-sizing cost constants (2-3 hours)**
`rightsizing_service.py` still uses `CPU_COST_PER_CORE_HOUR = 0.04` and `MEMORY_COST_PER_GB_HOUR = 0.005`. The `resource_pricing_worker` already runs daily and populates pricing data — it just isn't being queried by the right-sizing service. The fix is to replace the constant lookups with Redis reads, falling back to constants only on cache miss.

The verified update correctly identifies this: wire `_calculate_monthly_cost` to query Redis keys that `resource_pricing_worker` already populates. Check what key format that worker uses and match it in the service.

---

### Tier 3 — Enterprise Issues Still Unaddressed (10 of 13)

Of the 13 issues from the enterprise analysis, the verified update confirms only 3 are done (race condition, hibernation savings, partial AtharvaAI). The remaining 10 need prioritization. Here's the honest ordering based on what blocks enterprise sales vs. what's nice to have:

**Blocks enterprise deals immediately:**

Right-sizing pre-flight checks (#4) — the ASG detection and IP-change warning. This is a correctness issue. An enterprise customer applying right-sizing to an ASG node causes an outage. This needs to be before any production deployment. Estimate: 4-5 hours including the `describe_auto_scaling_instances` call, instance-store check, and the pre-flight response schema.

Cluster delete preconditions (#11) — orphaned Karpenter controllers are a real operational hazard. Estimate: 2 hours.

Approval gates for emergency hibernation (#5) — the emergency sleep endpoint can kill a production cluster with no oversight. Estimate: 3-4 hours to wire through the existing approval system.

Karpenter budget guards (#9) — the NodePool CRD limits are a 20-minute config change. The budget guard Celery task that auto-pauses is 3-4 hours. Do the CRD limits now, task later.

**Will come up in security review:**

Audit log tamper evidence (#8) — SHA256 checksum on `audit_logs` rows plus the periodic verification task. The S3 append-only sink for critical actions is genuinely important for SOC2. Estimate: 4-6 hours including the Celery verification task.

Multi-region Redis namespacing (#1) — the blacklist is region-namespaced already (confirmed done). What's still missing is the pricing collector being parameterized by account+region. For a customer with 3 accounts × 4 regions, 11 out of 12 region combinations are getting no pricing data. Estimate: 4-6 hours to restructure the pricing collector beat schedule.

**Can defer but shouldn't forget:**

AtharvaAI capacity check parallelization (#2) — the serial execution is a performance problem, not a correctness problem. Rankings still work, they're just slow and might expire. Estimate: 2 hours.

Cache invalidation on template update (#7) — 1 hour. The Redis delete call in `template_service.py` after save/update. Low effort, real UX improvement.

ML model versioning (#12) — the `ml_models` table already exists in the models layer. Wiring it and implementing shadow mode is 8-10 hours. Genuinely important for production ML but can follow the first enterprise deployment.

AWS API rate limiting (#13) — 4-6 hours for the Redis-based limiter. Important at scale, not day-one critical.

User preferences to DB (#10) — 1 hour. Frontend change in `Settings.jsx` + wire the existing endpoint. Lowest risk item on the list.

---

## Revised Timeline

Given what's verified done vs. what remains, the actual remaining work is less than the 38-hour estimate in the verified update:

**This week (must-do before any enterprise demo):**
- Hibernation DB columns migration: 30 min
- Celery schedule fix (spot prices): 15 min
- Right-sizing cost constants → real pricing: 2-3h
- Right-sizing pre-flight checks (ASG, IP, instance-store): 4-5h
- Cluster delete preconditions: 2h
- Karpenter NodePool CRD limits (the config part): 30 min
- Cache invalidation on template update: 1h

Total: ~12 hours

**Next week (before first enterprise customer onboards):**
- Approval gates for emergency hibernation: 3-4h
- Audit log tamper evidence (checksum + S3 sink): 4-6h
- AtharvaAI capacity check parallelization: 2h
- AWS API rate limiting: 4-6h
- User preferences to DB: 1h

Total: ~16 hours

**Within a month (for scale and compliance certification):**
- Multi-region pricing collector parameterization: 4-6h
- Karpenter budget guard Celery task: 3-4h
- ML model versioning + shadow mode: 8-10h

Total: ~18 hours

**Actual total remaining: ~46 hours**, down from the 38-hour estimate in the verified update (which was understated because it didn't account for the full scope of the 10 unaddressed enterprise issues).

---

## One Thing the Verified Update Gets Wrong

The verified update lists multi-region Redis namespacing as "not yet addressed" for issue #1. But the code inspection shows `risky_pools:{region}` is already in the pool ranking service — that part is done. What's genuinely missing from issue #1 is the **pricing collector parameterization by account+region** and **per-region beat scheduling**. Don't re-implement the blacklist namespacing; that's already there. Focus the multi-region work exclusively on the pricing data pipeline.