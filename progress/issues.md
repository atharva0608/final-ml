# Issue Log

| ID | Issue Description | Cause | Effect | Fix Implemented | Impact (After Fix) | Status |
|----|-------------------|-------|--------|-----------------|--------------------|--------|
| 1  | `ApiError: Inactive user account` (403 Forbidden) when fetching logs (`web_scraper`, `database`, `instance_manager`, etc.) | user account (likely `admin` or `client`) has `is_active=False` in the database. The seed script might verify existence but skip re-activation. | Frontend dashboard fails to load system logs and metrics. Multiple red errors in console. | **Modify `connection.py`**: Update `seed_test_users` function to explicitly set `is_active=True` even if the user already exists. | Dashboard components will successfully fetch logs. 403 errors resolved. | 🟡 Fixed (Need Redeploy) |
| 2  | **Client Adding Failed** | 1. 403 Inactive User (Backend) <br> 2. `api.js` local config missing `/api` prefix (Frontend) | Admin cannot onboard new clients. Requests fail with 403 or 404. | 1. **Fixed (Backend)**: User reactivation logic. <br> 2. **Fixed (Frontend)**: Updated `API_BASE_URL` in `api.js` to default to `.../api` in dev. | Admin can successfully create new client accounts. |  Fixed (Need Redeploy) |
| 3  | **Model Upload Failed** | `3-A` Auth/URL Config (see above). <br> `3-B` **Status 500 (Internal Server Error)**: Missing `python-multipart` in running container (Verified present in `requirements.txt`, implies stale build). | ML Models cannot be deployed. Workflow blocked. | **Verified Code**: Dependency exists in `requirements.txt`. **Action**: Rebuild container to install it. | Model upload works reliably. | 🟡 Fixed (Need Redeploy) |
| 4  | **Frontend Blank Screen / Auth Crash** | Likely unhandled exception in React lifecycle due to Auth failure. | User experience destroyed by sudden crashes to login screen. | Fixing the root cause (Auth 403) prevents the crash triggers. | Frontend handles errors gracefully without crashing/logging out. | 🟢 Fixed |
| 5 | **Model Upload Name Conflict** | Backend strictly rejects duplicate filenames (409 Conflict). | Upload fails. | **Modify `lab.py`**: Auto-rename logic. <br> **Fix 2**: Added `logger` import. <br> **Fix 3**: Fixed `AttributeError` on status enum. | Uploads succeed even if filename exists (auto-versioned). | 🟡 Fixed (Need Redeploy) |
| 6 | **Missing Model Action Buttons** | Frontend `ModelExperiments.jsx` only showed actions for `testing` status, but uploaded models are `candidate`. | User cannot Graduate/Reject new models. | **Fix `ModelExperiments.jsx`**: Updated conditions to include `candidate` status. | Buttons appear for new uploads. | 🟡 Fixed (Need Redeploy) |
| 7 | **Resource Distribution Mock Data** | `NodeFleet.jsx` used hardcoded mock data for the distribution pie chart. | Chart showed fake "Global Fleet" data instead of actual client nodes. | **Fix `NodeFleet.jsx`**: Replaced mock generator with `getRealDistribution` logic aggregation from API nodes. | Chart now reflects real fleet composition. | 🟡 Fixed (Need Redeploy) |
| 8 | **ML Production Flow Incomplete** | No "Deploy to Prod" endpoint exists; "Apply" button was mock; Health check didn't show active model. | User could graduate models but not actually activate them for the fleet. | **Feature**: Added `PUT /deploy` endpoint, updated `Controls.jsx` to call it, and patched `admin.py` to show active model in Health check. | Full flow: Upload -> Graduate -> Deploy -> Health Check is now working. | 🟡 Fixed (Need Redeploy) |
| 9 | **Missing Health Checks & ML in Prod** | Health checks were static/missing; `ClusterPipeline` ignored ML models. | System health was fake; Production optimization ignored ML risk scores. | **Feature**: Created `HealthMonitor` worker for Redis/DB/ML checks; Updated `ClusterPipeline` to use active ML model for pre-flight risk check. | Real-time health monitoring and safer production optimization. | 🟡 Fixed (Need Redeploy) |
| 10 | **Frontend Crash (ReferenceError)** | `Controls.jsx` accessed `activeModel` before initialization. | "System Controls" page crashed with white screen / error console. | **Fix `Controls.jsx`**: Reordered variable declarations to call hook before using state. | Page loads correctly. | 🟡 Fixed (Need Redeploy) |
| 11 | **Deploy Model Failed** | `deployModel` was missing from `api.js` default export. | clicking "Apply" caused `TypeError: deployModel is not a function`. | **Fix `api.js`**: Added `deployModel` to default export object. | Deployment action works. | 🟡 Fixed (Need Redeploy) |
